### how to generate `compile_commands.json`

```shell
$ sudo apt install bear
$ make lsp
```

### libassimp

Currently add this deps manually.
1. git clone source code into /tmp
2. build there
3. cp the include header and .so into thirdparty/assimp
